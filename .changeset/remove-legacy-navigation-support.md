---
"@heyo-sh/heyo-docs": major
"@heyo-sh/create-heyo-docs": major
---

Render declarative header navigation as `Button` links with a `variant` that defaults to `link`, and rename the primary button variant from `default` to `primary`.

Remove legacy CSS, Vite 5, Swagger 2, and Cloudflare `OPENAI_API_KEY` compatibility paths.
