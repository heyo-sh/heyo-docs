---
title: Code Blocks
description: Add clear, copyable commands and configuration examples.
---

# Code Blocks

Show one task per example. Add a language so the code is easy to read.

## Add a command

```bash
bun run dev
bun run build
```

## Add a file name

Use `CodeBlock` when the file name helps explain where the code belongs.

<CodeBlock language="ts" title="heyo-docs.config.ts" showLineNumbers>
  {`export default heyoDocs({
  title: "Orbit Docs",
  content: "content",
  theme: "grain",
});`}
</CodeBlock>

## Show alternatives

Use `CodeBlockGroup` for equivalent commands, such as package managers.

<CodeBlockGroup defaultValue="bun" variant="line">
  <CodeBlock language="bash" title="bun">
    {"bun add @heyo-sh/heyo-docs"}
  </CodeBlock>
  <CodeBlock language="bash" title="npm">
    {"npm install @heyo-sh/heyo-docs"}
  </CodeBlock>
  <CodeBlock language="bash" title="pnpm">
    {"pnpm add @heyo-sh/heyo-docs"}
  </CodeBlock>
</CodeBlockGroup>
